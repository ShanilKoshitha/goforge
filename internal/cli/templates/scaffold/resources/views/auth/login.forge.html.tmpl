@extends("layouts/app")
@section("title"){{.Title}}@endsection
@section("content")
  @component("components/card", title=.Title, tone="auth")
    <form action="/login" method="post">
      @csrf(form=.Form)
      @component("components/input", form=.Form, id="login-email", name="email", label="Email", recover=true, type="email", attributes(autocomplete="email", required=true))@endcomponent
      @component("components/input", form=.Form, id="login-password", name="password", label="Password", recover=false, type="password", attributes(autocomplete="current-password", required=true))@endcomponent
      <button type="submit">Sign in</button>
    </form>
    @slot("actions")<a href="/register">Create an account</a> · <a href="/forgot-password">Forgot password?</a>@endslot
  @endcomponent
@endsection
