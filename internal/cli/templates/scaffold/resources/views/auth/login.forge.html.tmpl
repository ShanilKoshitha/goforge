@extends("layouts/app")
@section("title"){{.Title}}@endsection
@section("content")
  @component("components/card", title=.Title, tone="auth")
    <form action="/login" method="post">
      @csrf
      <label>Email
        <input type="email" name="email" value="@old("email", "")" autocomplete="email" required>
      </label>
      @errors("email")<p role="alert">{{.}}</p>@enderrors
      <label>Password
        <input type="password" name="password" autocomplete="current-password" required>
      </label>
      @errors("password")<p role="alert">{{.}}</p>@enderrors
      <button type="submit">Sign in</button>
    </form>
    @slot("actions")<a href="/register">Create an account</a> · <a href="/forgot-password">Forgot password?</a>@endslot
  @endcomponent
@endsection
