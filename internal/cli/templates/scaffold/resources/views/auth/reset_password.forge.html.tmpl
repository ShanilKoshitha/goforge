@extends("layouts/app")
@section("title"){{.Title}}@endsection
@section("content")
  @component("components/card", title=.Title, tone="auth")
    @if(.Error)<p role="alert">{{.Error}}</p>@endif
    <form action="/reset-password" method="post">
      @csrf(form=.Form)
      <input type="hidden" name="token" value="{{.Token}}">
      @errors("token", form=.Form)<div id="reset-token-errors" role="alert">{{.}}</div>@enderrors
      @component("components/input", form=.Form, id="reset-password", name="password", label="New password", recover=false, type="password", attributes(autocomplete="new-password", minlength=12, required=true))@endcomponent
      @component("components/input", form=.Form, id="reset-password-confirmation", name="password_confirmation", label="Confirm new password", recover=false, type="password", attributes(autocomplete="new-password", minlength=12, required=true))@endcomponent
      <button type="submit">Reset password</button>
    </form>
    @slot("actions")<a href="/login">Back to sign in</a>@endslot
  @endcomponent
@endsection
