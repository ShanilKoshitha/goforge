@extends("layouts/app")
@section("title"){{.Title}}@endsection
@section("content")
  @component("components/card", title=.Item.Name, tone="resource")
    @if(.Notice)<p role="status">{{.Notice}}</p>@endif
    <form action="/app/[[.Plural]]/{{.Item.ID}}" method="post">
      @csrf
      @method("DELETE")
      <button type="submit">Delete</button>
    </form>
    @slot("actions")
      <a href="/app/[[.Plural]]/{{.Item.ID}}/edit">Edit</a>
      <a href="/app/[[.Plural]]">Back to [[.Plural]]</a>
    @endslot
  @endcomponent
@endsection
