@extends("layouts/app")
@section("title"){{.Title}}@endsection
@section("content")
  @component("components/card", title=.Title, tone="auth")
    <form action="/register" method="post">
      @csrf(form=.Form)
      @component("components/input", form=.Form, id="register-name", name="name", label="Name", recover=true, attributes(autocomplete="name", required=true))@endcomponent
      @component("components/input", form=.Form, id="register-email", name="email", label="Email", recover=true, type="email", attributes(autocomplete="email", required=true))@endcomponent
      @component("components/input", form=.Form, id="register-password", name="password", label="Password", recover=false, type="password", attributes(autocomplete="new-password", minlength=12, required=true))@endcomponent
      @component("components/input", form=.Form, id="register-password-confirmation", name="password_confirmation", label="Confirm password", recover=false, type="password", attributes(autocomplete="new-password", minlength=12, required=true))@endcomponent
      <button type="submit">Create account</button>
    </form>
    @slot("actions")<a href="/login">Already have an account?</a>@endslot
  @endcomponent
@endsection
