@props(title, tone="neutral")
<article @attributes(class="card", class=(printf "card--%s" $tone))>
  <header><h1>{{headline $title}}</h1></header>
  <div>@slot("default")@endslot</div>
  <footer>@slot("actions")@endslot</footer>
</article>
